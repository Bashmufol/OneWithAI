# Copy this file to .env and fill in the values.
# NEVER commit the .env file. Only commit this .env.example file.

# TODO (Abdulroheem + Bashir): Once the backend API is deployed on ECS,
# paste the ALB (Load Balancer) URL here.
# Example: VITE_API_URL=https://evocharge-api.us-east-1.elb.amazonaws.com
VITE_API_URL=
